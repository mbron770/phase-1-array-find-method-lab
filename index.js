// function superbowlWin(records) {
//     const win = records.find((record) => record.result === "W")
// return win ? win.year : undefined
//   }






function superbowlWin(arrays){
    const win = arrays.find((array) => array.result === "W")
return win ? win.year : undefined
}
